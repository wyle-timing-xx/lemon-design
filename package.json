{
  "name": "lemon-design-xx",
  "version": "1.0.0",
  "description": "A modern React component library built with MUI and TailwindCSS - lemon design system",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "dev": "lerna run dev --stream",
    "storybook": "lerna run storybook --scope=@lemon-design-xx/storybook",
    "build-storybook": "lerna run build-storybook --scope=@lemon-design-xx/storybook",
    "test": "lerna run test",
    "clean": "lerna clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "lerna": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
